import { createTheme } from "@mui/material/styles";
// import { red, lightRed } from "@mui/material/colors";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#db5e59",
    },
    secondary: {
      main: "#ed7470",
      midNightRed: "#b00802",
    },
  },
});
